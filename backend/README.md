# Backend — FastAPI

Backend del MVP de digitalización de remitos. Ver `../PROPUESTA_MVP_OCR_REMITOS.md` para el alcance funcional completo.

Responsabilidades:
- Exponer los endpoints de `/api/*` (clientes, remitos) y `/api/internal/*` (usados por n8n).
- Conectar con PostgreSQL.
- Conectar con Google Drive mediante una Service Account.

Setup: pendiente (se agrega en la Fase 1 de implementación).
